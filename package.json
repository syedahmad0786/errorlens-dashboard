{
  "name": "errorlens-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "ErrorLens — Error monitoring dashboard frontend",
  "scripts": {
    "dev": "npx serve . -l 3000 -s",
    "preview": "npx serve . -l 5000 -s"
  }
}
